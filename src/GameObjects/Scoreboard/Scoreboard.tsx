import React, { ReactComponentElement } from 'react'

type ScoreBoardProps = {
	type: 'pawn' | 'king',
	player: number,
}

export default function ScoreBoard({

}: ScoreBoardProps): ReactComponentElement<any, any> {

	return <div>

		{/* score-board */}
			{/* avatar */}
			{/* login info */}

	</div>
}
